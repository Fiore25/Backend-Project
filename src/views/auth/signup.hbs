<div class="cl1">
  <div>
    <div>
      <div>
        <h4 class="text-center">
          Si aún no tienes una cuenta regístrate!
        </h4>
      </div>
      <div>
        <form action="/auth/signup" method="POST">
          <div class="nom">
            <label for="name">Nombre:</label>
            <input
              type="text"
              class="ing"
              name="name"
              placeholder="Nombre"
              value="{{name}}"
            />
          </div>
          <div class="nom">
            <label for="email">Email:</label>
            <input
              type="email"
              class="ing"
              name="email"
              placeholder="Email"
              value="{{email}}"
            />
          </div>
          <div class="nom">
            <label for="password">Contraseña</label>
            <input
              type="password"
              class="ing"
              name="password"
              placeholder="Contraseña"
              value="{{password}}"
            />
          </div>
          <div class="nom">
            <label for="confirm_password">Repetir Contraseña:</label>
            <input
              type="password"
              class="ing"
              name="confirm_password"
              placeholder="Repetir Contraseña"
              value="{{confirm_password}}"
            />
          </div>
          <button>
            Regístrate!
          </button>
        </form>
      </div>
      <p class="text-center">¿Ya tienes una cuenta?
        <a href="/auth/signin" class="text-info">Inicia sesión</a></p>
    </div>
  </div>
</div>